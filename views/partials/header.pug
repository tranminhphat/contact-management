.navbar.navbar-light.fixed-top.navbar-expand-md
  .container
    a.navbar-brand(href='/')
      i.fas.fa-cube
      | #{__('My Web Application')}
    button.navbar-toggler(type='button', data-toggle='collapse', data-target='.navbar-collapse')
      span.sr-only Toggle navigation
      span.navbar-toggler-icon
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li.nav-item(class=(title === 'Home') ? 'active' : undefined)
          a.nav-link(href='/') #{__('Home')}
        li.nav-item(class=(title === 'Contact') ? 'active' : undefined)
          a.nav-link(href='/contact') #{__('Contact')} 
        li.nav-item(class=(title === 'Contact List') ? 'active' : undefined)
          a.nav-link(href='/contact-list') #{__('Contact List')} 
      ul.nav.navbar-nav.ml-auto
        if !user
          li.nav-item(class=(title === 'Login') ? 'active' : undefined)
            a.nav-link(href='/login') #{__('Login')}
          li.nav-item(class=(title === 'Create Account') ? 'active' : undefined)
            a.nav-link(href='/signup') #{__('Create Account')}
        else
          li.dropdown(class=(title === 'Account Management') ? 'active' : undefined).nav-item
            a.dropdown-toggle.nav-link(href='#', data-toggle='dropdown')
              if user.profile.picture
                img(src=user.profile.picture)
              else
                img(src=user.gravatar(60))
              span= user.profile.name || user.email || user.id
              i.caret
            ul.dropdown-menu
              li.dropdown-item
                a(href='/account') #{__('My Account')}
              li.divider.dropdown-item
              li.dropdown-item
                a(href='/logout') #{__('Logout')}
        li.dropdown.nav-item
         a.dropdown-toggle.nav-link(href='#', data-toggle='dropdown') #{__('English') === 'English' ? 'English' : 'Tiếng Việt'}
         ul.dropdown-menu
           a(href="/change-lang/en")
             li.dropdown-item #{__('English')}
           a(href="/change-lang/vi")
             li.dropdown-item #{__('Vietnamese')}
